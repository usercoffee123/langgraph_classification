[project]
name = "parking-lot"
version = "0.1.0"
description = "Analyze parking-lot images with YOLO, LangGraph, and Claude"
requires-python = ">=3.11"
dependencies = ["langchain-core>=1,<2", "langgraph>=1,<2", "langchain-anthropic>=1,<2", "python-dotenv>=1,<2", "ultralytics>=8.4,<9", "pillow>=12,<13"]

[tool.uv]
package = false
